import { extractColorsFromImageData } from "./extractColors";
import { FinalColor } from "./types/Color";
import type { BrowserOptions } from "./types/Options";
/**
 * Extract colors from an HTMLImageElement.
 */
declare const extractColorsFromImage: (image: HTMLImageElement, options?: BrowserOptions) => Promise<FinalColor[]>;
/**
 * Extract colors from a path.
 * The image will be downloaded.
 */
declare const extractColorsFromSrc: (src: string, options?: BrowserOptions) => Promise<FinalColor[]>;
/**
 * Extract colors from a picture.
 */
declare const extractColors: (picture: string | HTMLImageElement | ImageData | {
    data: Uint8ClampedArray | number[];
    width?: number;
    height?: number;
}, options?: BrowserOptions) => Promise<FinalColor[]>;
export { extractColorsFromImageData, extractColorsFromImage, extractColorsFromSrc, extractColors };
export default extractColors;
